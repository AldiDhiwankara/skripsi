const StudentRegistrationForm = ({ children }) => (
    <div className="min-h-screen flex justify-center items-center bg-[#133043]">
        <div className="w-full max-w-3xl">{children}</div>
    </div>
  );
  
  export default StudentRegistrationForm;
  